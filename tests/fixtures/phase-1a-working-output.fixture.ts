/** Contract-derived fixture only. It is not a Golden Reference or an official workbook. */
export const phase1aWorkingOutputFixture = Object.freeze({
  fixtureVersion: "phase-1a-working-output-v1",
  bytes: new Uint8Array([80, 75, 3, 4]),
});
