# Contracts

Repository-native interface artifacts belong here when introduced by an approved implementation slice:

- `application/`: use-case request/response contracts;
- `tools/`: controlled AI tool schemas;
- `exports/`: output/export contracts.

Canonical business authority remains under `docs/canonical/`. Files here implement that authority; they do not override it.
